%h1 Editing Product
.container
  = form_for(@product, url: admin_product_path(@product.permalink)) do |f|
    = render partial: 'form', locals: {f: f}
    .bottom_links
      = link_to 'Show', admin_product_path
      |
      = link_to 'Back', admin_products_path
